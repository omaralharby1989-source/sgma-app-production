# Store screenshots

These are SAFE sample screenshots of PUBLIC screens only (login, register) — no member PII,
no admin/super-admin content.

- `01-login.jpg` — login screen (Arabic RTL)
- `02-register.jpg` — new-account screen (Arabic RTL)

Google Play requires at least 2 phone screenshots. Before submission, add more screenshots of
in-app screens (news, articles, academy, profile) captured with a DEMO member account so no real
member data is shown. Recommended phone size: 1080×1920 (or similar 9:16). Export from a device or
emulator running the release build.
